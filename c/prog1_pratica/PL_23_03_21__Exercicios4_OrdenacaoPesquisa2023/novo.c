/*
 * define max
 * typedef COLABORADOR
 *
 * main:
 * menu
 * ate ao 5 (sorts que demos)
 *
 *
 * passar array e tamanho
 * (colabs, &numColabs)
 * (func, &tot)
 *
 * listaInfo(func, tot)
 *
 *
 * */


/*
    // NUM
    novo.numero = *numColabs + 1;
    printf("\nO novo colaborador vai ter o nr %d", novo.numero);

    // NOME
    printf("Nome");
    scanf("%[^\n]s", novo.nome);

    // NIF
    scanf("%ld", &novo.nif);

    // SALARIO
    scanf("%f", &novo.salario);

    // GUARDA ARRAY
    colabs[*numColabs] = novo;
    *numColabs = *numColabs + 1;
    printf("Inserido com sucesso!\n");*/