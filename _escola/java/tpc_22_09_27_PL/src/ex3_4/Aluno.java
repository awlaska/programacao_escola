package ex3_4;

public class Aluno {
    // a. Três atributos de acesso privado: ‘Nome’, ‘Cidade’, ‘email’ do tipo String;
    // b. Dois atributosdeacessoprivado:‘Idade’e‘NumeroCartao’dotipo Int;
    // c. Um array de acesso privado: ‘Notas’ do tipo Double. Este array deve ser
    // inicializado com 5 posições;
    // d. Um construtor de instâncias da classe, sem argumentos;
    // e. Um construtor de instâncias da classe que recebe por argumento
    // informação para todos os atributos, exceto o array notas;
    // f. Os métodos Getter e Setter para os atributos;
    // g. O método toString(), de modo a que este apresente a informação do
    // aluno;
    // h. O método addNota(…), que adiciona uma nota ao aluno;
    // i. O método obterMedia(…) que calcula e devolve a média das notas do
    // aluno.
    // j. O método obterMelhorNota(…) que devolve amelhor nota do aluno.
}
