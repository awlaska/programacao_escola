package ex1_2;

public class Stand {
    // a. Um array,‘veiculos’,dotipo Veículo(codificadonoexercícioanterior);
    Veículo veiculos[] = new Veículo[];

    // b. Um construtor de instâncias da classe, sem argumentos, que inicializa o
    // array para 50 valores;
    public static void Stand(){
        veículos[].length = 50;
    }

    // c. Os métodos Getter e Setter para o array;
    // d. Ométodo adicionarVeiculo(…)que recebeumVeículo e acrescenta-oao
    // array;
    // e. O método obterVeiculo(…) que recebe o Modelo do Veículo e devolve
    // esse veículo;
    // f. O método obterQuantidadeVeiculos(…) que devolve a quantidade de
    // veículo existentes no stand;
    // g. O método listarVeiculos(…) que lista todos os veículos de uma dada
    // marca. Se não existir nenhum veículo da marca, deverá apresentar a
    // mensagem “O stand não possui veículos da marca «marca»”, onde
    // «marca» deve ser substituída pela marca passada por argumento.
}
