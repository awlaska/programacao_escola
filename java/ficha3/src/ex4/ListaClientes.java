package ex4;

public class ListaClientes {
    //TODO i. Um array privado para clientes, inicializado para 50 clientes;
    //TODO ii. O construtor de instâncias da classe;
    //TODO iii. O Getter e o Setter para o array;
    //TODO iv. O método adicionarCliente(…) que recebe um cliente e adiciona- o ao array clientes.
    // O cliente só deve ser adicionado caso não exista nenhum outro cliente registado com o mesmo NIF.
    // Se existir um NIF duplicado, deverá ser lançada a exceção NIFRepetidoException (esta exceção deve ser criada).
    //TODO v. O método procurarClientePorNIF(…) que recebe o NIF por parâmetro e devolve o cliente que possui esse NIF.
    // Se não existir nenhum cliente com o NIF dado,
    // deverá lançar a exceção ClienteNaoExisteException (esta exceção deve ser criada).
}
